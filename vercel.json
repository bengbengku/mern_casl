{
  "version": 2,
  "builds": [
    {
      "src": "set DEBUG=anggiat-server:* && node ./bin/www",
      "use": "@now/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "set DEBUG=anggiat-server:* && node ./bin/www"
    }
  ]
}
